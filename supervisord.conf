[supervisord]
nodaemon=true                    ; start in foreground if true; default false

[program:postfix]
command=/usr/sbin/postfix start-fg              ; the program (relative uses PATH, can take args)

[program:rsyslog]
command=rsyslogd -n              ; the program (relative uses PATH, can take args)
